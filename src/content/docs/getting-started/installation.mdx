---
title: Installation
description: Install the Azul CLI and Studio plugin.
---

import { Aside } from "@astrojs/starlight/components";

Azul has two parts you need:

- **Daemon (CLI)** (`azul`): runs on your machine and syncs files.
- **Companion plugin**: runs in Roblox Studio and connects to the daemon.

You must install both before starting your first sync session.

## Installing the Azul Daemon

### Recommended install (npm)

1. Install Node.js from [nodejs.org](https://nodejs.org/). It includes `npm`, which is used to install the Azul CLI.
2. Install Azul globally:

   ```bash
   npm install -g azul-sync
   ```

3. Verify the install:

   ```bash
   azul --version
   ```

### Manual install (from source)

<details>
<summary>Click to expand</summary>

1. Clone the repository or download the ZIP and extract it.
2. Install Node.js from [nodejs.org](https://nodejs.org/) (or package manager):

   ```bash
   # Windows (using winget)
   winget install OpenJS.NodeJS.LTS
   # macOS (using Homebrew)
   brew install node
   # Linux (using apt)
   sudo apt install nodejs npm
   ```

3. Install dependencies:

   ```
   npm install
   ```

4. Build the project:

   ```
   npm run build
   ```

5. Install globally from the local checkout:

   ```
   npm install -g .
   ```

</details>

## Installing the Roblox plugin

1. Install from the Creator Store: [Azul Companion Plugin](https://create.roblox.com/store/asset/79510309341601/Azul-Companion-Plugin)

<Aside type="note">
  If the plugin does not appear in Studio, see [plugin
  troubleshooting](https://github.com/Ransomwave/azul/tree/main/plugin#troubleshooting).
</Aside>

## Recommended tooling

While these tools aren't required to use Azul, they are highly recommended to get the best experience out of Azul:

#### Luau LSP

Use the [Luau Language Server extension](https://marketplace.visualstudio.com/items?itemName=JohnnyMorganz.luau-lsp) for [VSCode](https://code.visualstudio.com/). This extension provides Luau IntelliSense, error checking, and more.

It works by reading the `sourcemap.json` file generated by Azul to understand the structure of your project and provide accurate suggestions and error checking.

To get Luau-LSP working with Azul, open your `User Settings (JSON)` from the command palette (`Ctrl+Shift+P` or `Cmd+Shift+P`) and make sure to set up luau-lsp like this:

```json
{
  "luau-lsp.plugin.enabled": true,
  "luau-lsp.sourcemap.enabled": true,
  "luau-lsp.sourcemap.autogenerate": false,
  "luau-lsp.sourcemap.sourcemapFile": "sourcemap.json",
  "luau-lsp.sourcemap.includeNonScripts": true
}
```

Luau-LSP is also available for other IDEs like [IntelliJ](https://plugins.jetbrains.com/plugin/24957-luau) and [Neovim](https://github.com/lopi-py/luau-lsp.nvim), but setup may vary.

#### Verde

[Verde](https://marketplace.visualstudio.com/items?itemName=Dvitash.verde) is a great VSCode extension that mimics the Roblox Studio Explorer and Properties windows. It works great alongside Azul to provide a seamless development experience.
